# calcular raiz quadrada
"""
1° forma de usar a biblioteca imporatndo tudo
import math

n = int(input('digite um valor: '))
raiz = math.sqrt(n)
print('A raiz de {} é igual a {:.2f}'.format(n, raiz))
"""
"""from math import sqrt

n = int(input('digite um valor: '))
raiz = sqrt(n)
print('A raiz de {} é igual a {:.2f}'.format(n, raiz))"""
"""
para saber quais bibliotecas tem no python, abre o google , pesquisa PYTHON, e vai em docs, volta no pycharm e 
confira a versao do pycharm, depois no google, vai em libraries-referencs (dentro de docs, no site do Python)
"""
"""
import random
#n = random.random()
n = random.randint(1, 10)
print(n)
"""
# import <ctrl space>(mostra tudo q da pra usar no pycharm)
"""
import emoji

print(emoji.emojize('Ola, Mundo :sunglasses:', use_aliases=True))
python - interpreter settings
"""
